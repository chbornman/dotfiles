# Window rules - Hyprland 0.53+ syntax
# Make Zen Browser fully opaque (override firefox-based-browser tag)
windowrule = opacity 1.0 1.0, match:class zen

# Floating windows
windowrule = float on, match:class org\.rncbc\.qpwgraph

# Force Tile
windowrule = tile on, match:class localsend
