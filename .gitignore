# Neovim
nvim/.config/nvim/lazy-lock.json

# Claude Code sensitive files (keep structure but ignore local overrides if desired)
# claude/.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db
machines/asahi/.local/bin/obsidian
