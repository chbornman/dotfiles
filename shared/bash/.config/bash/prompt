# User-specific bash prompt configuration
# This overrides the omarchy default prompt

# Simple prompt showing only current folder name and arrow
PS1='\[\e[36m\]\W\[\e[0m\] → '

# Add current working directory to terminal title
PS1="\[\e]0;\w\a\]$PS1"