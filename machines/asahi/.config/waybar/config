{
    "layer": "top",
    "position": "bottom",
    "height": 24,
    "spacing": 0,

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["clock"],
    "modules-right": ["custom/screencast", "pulseaudio", "memory", "cpu", "custom/battery", "network", "bluetooth"],

    "sway/workspaces": {
        "disable-scroll": false,
        "all-outputs": false,
        "format": "{index}"
    },

    "sway/mode": {
        "format": "{}"
    },

    "clock": {
        "format": "{:%a %d %b  %H:%M}",
        "tooltip-format": "{:%Y-%m-%d %H:%M:%S}",
        "on-click": "zenity --calendar --width=300 --height=300"
    },

    "pulseaudio": {
        "format": "vol {volume}%",
        "format-muted": "vol muted",
        "on-click": "~/.local/bin/audio-menu",
        "on-click-right": "~/.local/bin/term-title wiremix",
        "on-scroll-up": "pactl set-sink-volume @DEFAULT_SINK@ +2%",
        "on-scroll-down": "pactl set-sink-volume @DEFAULT_SINK@ -2%"
    },

    "memory": {
        "interval": 3,
        "format": "mem {percentage}%",
        "on-click": "~/.local/bin/term-title btop"
    },

    "cpu": {
        "interval": 3,
        "format": "cpu {usage}%",
        "on-click": "~/.local/bin/term-title btop"
    },

    "custom/battery": {
        "exec": "~/.local/bin/battery-info",
        "interval": 10,
        "return-type": "json",
        "format": "{}",
        "on-click": "~/.local/bin/power-menu"
    },

    "network": {
        "interval": 5,
        "format-wifi": "wifi {essid}",
        "format-ethernet": "eth {ipaddr}",
        "format-disconnected": "wifi off",
        "tooltip-format": "{ifname}: {ipaddr}/{cidr}",
        "on-click": "~/.local/bin/wifi-menu",
        "on-click-right": "~/.local/bin/term-title impala"
    },

    "bluetooth": {
        "format": "bt {status}",
        "format-connected": "bt {device_alias}",
        "format-disabled": "bt off",
        "on-click": "~/.local/bin/bt-menu",
        "on-click-right": "~/.local/bin/term-title bluetui",
        "tooltip-format": "{controller_alias}\n{device_enumerate}"
    },

    "custom/screencast": {
        "exec": "pgrep wf-recorder && echo 'REC' || echo ''",
        "interval": 1,
        "format": "{}",
        "on-click": "pkill wf-recorder || wf-recorder -f ~/Videos/screencast-$(date +%Y%m%d-%H%M%S).mp4"
    }
}
